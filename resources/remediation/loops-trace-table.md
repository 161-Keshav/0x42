# Trace the last iteration

Python loop range stopping boundaries and excluded upper limits. Use a trace table to reason about off-by-one iteration mistakes.

## Practice

Make a table of values for range(1, 4) and range(1, 5). What changes? Explain which upper boundary is excluded.

## Teacher note

Compare a prediction against a trace table and discuss the stopping condition.
